cd checks/
git branch  # output will be * master

git branch new-feature
git branch             # output will be * master New feature

git checkout new-feature  # switched branch to new-feature

git checkout -b even-better-feature   # created breanch even-better-feature and switch to it

cd checks
git status
ls -l

git checkout master   #switch to master branch
git log -2
ls -l
git branch     # shows available branches

git branch -d new-feature   # deletes new-feature brannch
git branch -d even-better-feature # deletes even-better-feature branch