cd scripts
atom all_checks.py

git status
git checkout all_checks.py  # It reverts changes to modified files before they are staged.
git status

./all_checks.py > output.txt
git add *
git status

git reset HEAD output.txt  #   use "git reset HEAD <file>..." to unstage
git status
git commit -m "it should be os.path.exists"

# Amending commits

cd scripts/
touch auto-update.py
touch gather-information.sh
ls -l

git add auto-update.py
git commit -m 'Add two new scripts'

git add gather-information.sh
git commit --amend -m "add 2 new scripts"   # amend overwrites previous commits
# do not amend commits that have already been made public

# Rollbacks

cd scripts
kate all_checks.py  #make changes
git commit -a -m 'Add call to disk_full function'  # add and commit changes

git revert HEAD
git log -p -2